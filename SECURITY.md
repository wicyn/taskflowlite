# 安全策略

## 支持的版本

| 版本   | 是否支持          | 说明                              |
| ------ | ----------------- | --------------------------------- |
| 3.0.x  | :white_check_mark: | 当前稳定版本，接受安全修复        |
| 2.0.x  | :warning:          | 仅接受关键安全修复，建议升级到 3.x |
| < 2.0  | :x:                | 不再维护                          |

## 安全范围

TaskflowLite 是一个 header-only 的并发任务调度库，不提供运行时服务或网络接口。
本策略关注的安全问题，主要是可能导致**未定义行为或内存损坏**的缺陷，包括：

- 无锁数据结构（Chase-Lev deque、`FreeStack48/128` 等）的正确性缺陷，
  例如 ABA、释放后使用，以及错误的内存序导致的数据竞争。
- 内存池（`SegmentedPool` / `ObjectPool`）的越界、双重释放或对齐错误。
- 任务图执行中导致悬垂引用或异常传播失控的逻辑缺陷。
- 构建配置（CMake / 宏）在受支持平台上触发未定义行为的问题。

以下问题通常不属于本策略范围：普通功能缺陷、性能问题、仅影响文档的问题，
以及仅发生在项目未支持的平台或编译器上的问题。此类问题请提交普通 issue。

## 报告安全漏洞

请**不要**通过公开 issue 披露漏洞细节。请使用以下私密渠道之一：

1. **GitHub 私密上报（推荐）**：打开仓库的 `Security` → `Report a vulnerability`。
2. **邮件**：[qq978358810@gmail.com](mailto:qq978358810@gmail.com)

上报时请尽量提供以下信息：

- 受影响的版本与平台（操作系统、编译器和标准库）。
- 最小可复现示例，以及触发问题所需的线程数或调度场景。
- 如果由 sanitizer 发现，请附上 ASan、TSan 或 UBSan 报告。

## 响应流程

| 阶段       | 目标时限       |
| ---------- | -------------- |
| 确认收到   | 3 个工作日内   |
| 初步评估   | 7 个工作日内   |
| 修复并发布 | 视严重程度而定 |

我们采用**协调披露**：在修复发布前，请勿公开漏洞细节。修复合入后，
我们会在 release notes 和本仓库的 Security Advisory 中致谢报告者，除非对方要求匿名。
